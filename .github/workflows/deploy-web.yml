name: Deploy CloudChef Web (Vite)

on:
  push:
    branches: ["main"]
    paths:
      - "web/**"
      - ".github/workflows/deploy-web.yml"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      # 🔍 DEBUG STEP (DO NOT SKIP THIS)
      - name: Debug web/package.json
        working-directory: web
        run: |
          echo "===== COMMIT ====="
          git rev-parse HEAD
          echo "===== FILE CONTENT (line numbers) ====="
          cat -n package.json
          echo "===== TRY JSON PARSE ====="
          node -p "JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts.start"

      - name: Install
        working-directory: web
        run: npm install

      - name: Build
        working-directory: web
        env:
          VITE_API_BASE: https://cloudchef-api-bpbpc5fadph9ead3.uksouth-01.azurewebsites.net/api
        run: npm run build

      - name: Create deploy zip
        run: |
          cd web
          zip -r ../webapp.zip dist server.cjs package.json package-lock.json

      - name: Deploy to Azure Web App (Publish Profile)
        uses: azure/webapps-deploy@v2
        with:
          app-name: cloudchef-webapp
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: webapp.zip
